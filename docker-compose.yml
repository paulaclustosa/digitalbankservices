services:
  postgres:
    container_name: postgres
    image: postgres:14.1-alpine
    environment:
      POSTGRES_USER: paulaletscode
      POSTGRES_PASSWORD: password
      PGDATA: /data/postgres
    volumes:
      - postgres:/data/postgres
    ports:
      - "5432:5432"
    networks:
      - postgres
    restart: unless-stopped

  pgadmin:
    container_name: pgadmin
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-pgadmin4@pgadmin.org}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-admin}
      PGADMIN_CONFIG_SERVER_MODE: 'false'
    volumes:
      - pgadmin:/var/lib/pgadmin
    ports:
      - "5050:80"
    networks:
      - postgres
    restart: unless-stopped

    #  account:
    #    container_name: account
    #    image: digitalbankservices/account:latest
    #    environment:
    #      - SPRING_PROFILES_ACTIVE=docker
    #    ports:
    #      - "9080:9080"
    #    networks:
    #      - spring
    #      - postgres
    #    depends_on:
    #      - postgres

    #  user:
    #    container_name: user
    #    image: digitalbankservices/user:latest
    #    environment:
    #      - SPRING_PROFILES_ACTIVE=docker
    #    ports:
    #      - "9081:9081"
    #    networks:
    #      - spring
    #      - postgres
    #    depends_on:
    #      - postgres

networks:
  postgres:
    driver: bridge
    #  spring:
    #    driver: bridge

volumes:
  postgres:
  pgadmin:




